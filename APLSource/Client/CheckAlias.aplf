 {r}←CheckAlias(path alias);ns;filename;alisse
 r←⍬
 :If 0<≢alias
     filename←path,Reg.BuildListFilename{('/',⍺)/⍨⍺≢(-≢⍺)↑⍵}path
 :AndIf F.IsFile filename
     ns←⎕JSON⍠('Dialect' 'JSON5')⊣⊃F.NGET filename
 :AndIf 0<≢alisse←{⍵/⍨'@'∊¨⍵}ns.packageID
     ('Package alias "',alias,'" already in use')Assert~(⊂alias)∊1⊃∘SplitAtAlias¨alisse
 :EndIf
⍝Done
