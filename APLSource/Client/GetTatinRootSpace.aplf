 r←GetTatinRootSpace targetSpace;root
⍝ `targetSpace` is where the link to a package it going to be created,
⍝ That rules whether we return `#` or `⎕SE` as the root.
⍝ As a side effect the function makes sure that the space exists.
 targetSpace←⍕targetSpace
 :If '#'=1⍴targetSpace
     r←'#._tatin'
     root←#
 :ElseIf '⎕SE'≡1 ⎕C 3⍴targetSpace
     r←'⎕SE._tatin'
     root←⎕SE
 :Else
     'Invalid right argument'⎕SIGNAL 11
 :EndIf
 :If 0=⎕NC r
     r ⎕NS''
 :EndIf
⍝Done
