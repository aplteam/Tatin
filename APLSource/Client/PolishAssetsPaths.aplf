 PolishAssetsPaths←{
     asset←⍵
     0=≢asset:asset
     pkgPath←F.EnforceSlash ⍺
     asset←pkgPath{⍵↓⍨(≢⍺)×⍺≡(≢⍺)↑⍵}F.EnforceSlash asset
     0=+/b←F.IsDir¨asset:asset
     b∧←b\'/'≠⊃¨¯1↑¨b/asset
     0=+/b:asset
     (b/asset)←(b/asset),¨'/'
     asset
 }
