 ReadPackageConfigFile←{
     cfg_file←⍵,'/',CFG_NAME
     'Config file missing'Assert F.Exists cfg_file:
     cfg←InitPackageConfig''
     ⍝ load in actual
     buff←⊃F.NGET cfg_file
     _←'cfg'⎕NS ⎕JSON⍠('Dialect' 'JSON5')⊣buff
     cfg
 }
