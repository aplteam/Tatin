 ValidateAPI←{
     api←,⍵.api
     0=≢api:⍵
     apis←','(≠⊆⊢)api
     f1←0≠⊃∘(⎕NS'').⎕NC¨apis            ⍝ Valid names?
     f2←∨/¨apis∊¨⊂'⎕#'                 ⍝ No absolute refs?
     '"api" is invalid'Assert 0=f1+f2:
     ⍵
 }
