 ValidateVersion←{
     '"version" is not a string'Assert 0 2∊⍨10|⎕DR ⍵:                                   ⍝ Must be char
     '"version" is invalid'Assert 2≤'.'+.=⍵:                                            ⍝ At least two dots
     '"version" is invalid'Assert 0≠≢'^\d+\.\d+\.\d+(?:-\w+)?$'⎕S 0⊢(+/3>+\⍵='.')↑⍵:    ⍝ Must be {no}.{no}.{no} but everything after a third dot is ignored
     1:shy←0
 }
