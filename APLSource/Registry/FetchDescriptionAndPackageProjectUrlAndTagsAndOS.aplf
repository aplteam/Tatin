 FetchDescriptionAndPackageProjectUrlAndTagsAndOS←{
     path←⍵
     cfg←ReadConfigFile path,'/',CFG_Name
     OSes←⊃{0=≢⍵:'' ⋄ {⍺,', ',⍵}/⍵}cfg.(os_lin os_mac os_win)/'Lin' 'Mac' 'Win'
     0=cfg.⎕NC'info_url':cfg.description''OSes cfg.tags  ⍝ Because info_url was introduced after packages were already published
     cfg.(description info_url),OSes cfg.tags
 }
