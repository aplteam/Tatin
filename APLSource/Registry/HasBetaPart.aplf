 HasBetaPart←{
⍝ Returns a 1 when ⍵ carries a beta part like "-beta-2", otherwise a 0.
⍝ Neither a group nor a package name may carry a hyphen, so anything beyond the second one can
⍝ only be the beta part - which is what `RemoveBetaPart` relies on as well.
     1<≡⍵:∇¨⍵
     2<'-'+.=⍵
 }
