 PutRegistryIndex←{
     (tmp tgt)←(⊂⍵,'/',2⊃⎕NPARTS GetIndexFilename),¨'.tmp' '.txt'
     data←⍺
     data,←(##.LF≠¯1↑data)/##.LF
     _←(⊂data)##.F.NPUT tmp
     _←##.F.DeleteFile tgt
     _←tgt ⎕NMOVE tmp
     1:shy←0
 }
