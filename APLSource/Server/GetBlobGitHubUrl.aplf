 r←GetBlobGitHubUrl(group repo tag path)
⍝ Composes the URL that shows a file of a GitHub repository in its context, at a given tag.
⍝ That is what a README is credited to, and where the links inside one are pointed at.
 r←'https://github.com/',group,'/',repo,'/blob/',tag,'/',path
⍝Done
