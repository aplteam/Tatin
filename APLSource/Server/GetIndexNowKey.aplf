 r←GetIndexNowKey
⍝ Returns the IndexNow key from the INI file, or '' when there is none.
⍝ An empty key switches the whole thing off: nothing is served and nothing is announced.
 r←⊃''G.INI.Get'CONFIG:IndexNowKey'
⍝Done
