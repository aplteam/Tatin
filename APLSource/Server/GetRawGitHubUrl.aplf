 r←GetRawGitHubUrl(group repo tag path)
⍝ Composes the URL that serves a file from a GitHub repository as it is, at a given tag.
⍝ That is where a README is fetched from, and where the images inside one are pointed at.
 r←'https://raw.githubusercontent.com/',group,'/',repo,'/',tag,'/',path
⍝Done
