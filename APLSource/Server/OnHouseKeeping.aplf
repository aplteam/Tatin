 OnHouseKeeping←{
⍝ Purely used to detect if ∆SHUTDOWN and if so bring the program to a halt in developemnt
     0=∆SHUTDOWN:shy←0
     _←⎕EX'∆SHUTDOWN'
     0=##.APLTreeUtils.IsDevelopment:shy←#.Plodder.Shutdown G.INI  ⍝ Will ⎕OFF in runtime
     _←⎕NQ'#'QuitDQ
     0
 }
