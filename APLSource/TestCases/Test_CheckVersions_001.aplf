 r←Test_CheckVersions_001(stopFlag batchFlag);⎕TRAP;version
⍝ Run CheckVersions on an installed packages that is the best anyway
 ⎕TRAP←(999 'C' '. ⍝ Deliberate error')(0 'N')
 r←T._Failed

 version←TC.CheckForBetterVersion'TestData\Packages\Local-No-Better'
 →T.PassesIf 0=≢version

 r←T._OK
⍝Done
