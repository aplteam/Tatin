 r←Test_ClientConfig_105(stopFlag batchFlag);⎕TRAP;tempDir;settings;reg;res
⍝ Remove the one and only Registry
 ⎕TRAP←(999 'C' '. ⍝ Deliberate error')(0 'N')
 r←T._Failed

 tempDir←GetTempDir''
 F.DeleteFile tempDir,'/',TC.UserSettings.cfg_name                           ⍝ Precaution

 settings←⎕NEW TC.UserSettings(,⊂tempDir)
 →T.GoToTidyUp 0≠≢settings.registries

 reg←⎕NEW TC.DefineRegistry(,⊂'https://tatin.dev')
 settings.AddRegistry reg
 →T.GoToTidyUp 1≠≢settings.registries

 →T.GoToTidyUp 1≠res←settings.RemoveRegistry'https://tatin.dev'
 →T.GoToTidyUp 0≠res←settings.RemoveRegistry'https://tatin.dev'

 r←T._OK

∆TidyUp:
 F.RmDir tempDir
