 r←Test_ListPkgs_012(stopFlag batchFlag);⎕TRAP;list
⍝ List all packages for a Registry using an alias
 ⎕TRAP←(999 'C' '. ⍝ Deliberate error')(0 'N')
 r←T._Failed

 list←'aplteam'P.ListPackages'[local]'
 →T.PassesIf∧/'aplteam-'{⍺∘≡¨(≢⍺)↑¨⍵}list

 r←T._OK
⍝Done
