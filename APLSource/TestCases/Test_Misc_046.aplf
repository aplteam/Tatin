 r←Test_Misc_046(stopFlag batchFlag);⎕TRAP;list;expected
⍝ Exercise FindDependencies on a nested folder structure with both level←0 and level←1
⍝ Was once a bug
 ⎕TRAP←(999 'C' '. ⍝ Deliberate error')(0 'N')

 r←T._Failed

 list←##.Client.FindDependencies(##.CiderConfig.HOME,'/TestData/FindTatinDependencies2')'OS'
 expected←'aplteam-FilesAndDirs' 'aplteam-MakeHelpers'
 →T.PassesIf expected≡{Reg.RemoveVersionNumber⊃,/1↓⎕NPARTS ⍵}¨list

 list←1 ##.Client.FindDependencies(##.CiderConfig.HOME,'/TestData/FindTatinDependencies2')'OS'
 expected←'aplteam-OS-3.0.1' 'aplteam-OS-3.0.1'
 →T.PassesIf expected≡{⊃,/1↓⎕NPARTS ⍵}¨list

 r←T._OK
