 r←Test_Misc_090(stopFlag batchFlag);⎕TRAP;dir;filename
⍝ Exercise `Client.CreateUserCommandsReference`
 ⎕TRAP←(999 'C' '. ⍝ Deliberate error')(0 'N')
 r←T._Failed

 dir←GetTempDir ⍬
 filename←TC.CreateUserCommandsReference dir
 ∘∘∘
 →T.PassesIf
 →T.PassesIf

 r←T._OK

∆TidyUp:
 F.RmDir dir
⍝Done
