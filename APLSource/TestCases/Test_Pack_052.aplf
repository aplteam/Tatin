 r←Test_Pack_052(stopFlag batchFlag);⎕TRAP;path;zipFileName;list
⍝ Pack a package that is a Cider project but we specify a different folder (takes precedence)
 ⎕TRAP←(999 'C' '. ⍝ Deliberate error')(0 'N')
 r←T._Failed

 path←∆GetProjectPath,'/TestData/Build/Special/'
 zipFileName←'dependencies'TC.Pack path
 list←TC.ZipArchive.ListZipContents zipFileName
 →T.GoToTidyUp~(⊂F.EnforceSlash'apl-dependencies.txt')∊F.EnforceSlash¨list

 r←T._OK

∆TidyUp:
 ⎕NDELETE zipFileName
⍝Don
