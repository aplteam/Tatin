 r←Test_Server_404(stopFlag batchFlag);⎕TRAP;html
⍝ Ask for a the details of a particular package ID; pretends to be a Browser
⍝ Supposed to deliver the best one
 ⎕TRAP←(999 'C' '. ⍝ Deliberate error')(0 'N')
 r←T._Failed

 html←∆GetHtmlFromServer'localhost/v1/packages/details/aplteam-APLTreeUtils'
 →T.PassesIf∨/'<h2>Latest details of package "aplteam-APLTreeUtils"</h2>'⍷html

 r←T._OK

∆TidyUp:
⍝Done
