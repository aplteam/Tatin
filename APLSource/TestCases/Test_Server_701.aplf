 r←Test_Server_701(stopFlag batchFlag);⎕TRAP;c;q;res
⍝ Exercise the HEAD method on a package
 ⎕TRAP←(999 'C' '. ⍝ Deliberate error')(0 'N')
 r←T._Failed

 r←T._NotImplemented
 :Return

 c←R.NewClient 0
 c.Host←'localhost'
 q←R.NewRequest 0
 q.URI←'aplteam-Compare-5.0.0'
 q.Method←'HEAD'
 res←c TC.SendAndReceive q
 ∘∘∘

 r←T._OK
⍝Done
