 r←Test_Server_901(stopFlag batchFlag);⎕TRAP;c;q;response
⍝ Sends "list-commands" to the server.
 ⎕TRAP←(999 'C' '. ⍝ Deliberate error')(0 'N')
 r←T._Failed

 c←TC.NewClient'https://localhost'
 q←R.NewRequest 0
 q.URI←'/v1/list-commands'
 response←c TC.SendAndReceive q
 →T.PassesIf 200=response.StatusCode
 →T.PassesIf∨/'list-commands'⍷response.Content
 →T.PassesIf 0<','+.=response.Content

 r←T._OK
⍝Done
