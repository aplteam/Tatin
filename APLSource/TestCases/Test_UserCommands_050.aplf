 r←Test_UserCommands_050(stopFlag batchFlag);⎕TRAP;res;msg;rc;path
⍝ Exercise ]Tatin.CheckForBetterVersion with the -major flag
 ⎕TRAP←(999 'C' '. ⍝ Deliberate error')(0 'N')
 r←T._Failed

 path←'./TestData/Packages/Update-Check-Multiple'
 (rc msg res)←∆UCMD'CheckForBetterVersion ',path,' -major'
 →T.PassesIf 0=rc
 →T.PassesIf 0=≢msg
 →T.PassesIf 2=≢res
 →T.PassesIf∧/(∊res)∘{∨/⍵⍷⍺}¨'aplteam-MarkAPL-10.0.0' 'aplteam-EventCodes-2.1.2'

 r←T._OK
