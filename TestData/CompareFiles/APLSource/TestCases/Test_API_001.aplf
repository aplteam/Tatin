 R←Test_API_001(stopFlag batchFlag);⎕TRAP;msg;r;tool;parms;rc;out;err
⍝ Run "BeyondCompare" with just default parameters.
 ⎕TRAP←(999 'C' '. ⍝ Deliberate error')(0 'N')
 R←T._Failed

 :If batchFlag
     →0,R←T._NoBatchTest
 :EndIf

 msg←''
 msg,←⊂'This test case needs you: it fires up BeyondCompare without parameters.'
 msg,←⊂'No file should be editable; please check.'
 ∆Show msg

 parms←⎕SE.CompareFiles.ComparisonTools.CreateParmsForBeyondCompare
 parms.(use name)←⎕SE.CompareFiles.EstablishCompareEXE'BeyondCompare'
 parms.file1←##.CiderConfig.HOME,'/TestData/file1 with blank.txt'
 parms.file2←##.CiderConfig.HOME,'/TestData/file2 with blank.txt'
 (rc out err)←⎕SE.CompareFiles.Compare parms
 Assert 0=rc

 :If 1 ∆YesOrNo'Was "BeyondCompare" started, and neither file was editable?'
     R←T._OK
 :EndIf
