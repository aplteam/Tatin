(
 '* 0.6.2 from 2023-02-27'
 '  * Bug fixes                 '
 '    * `CompileVersionNumberPattern` did not find `HOME` in case no `⍺` was passed'
 '* 0.6.1 from 2023-02-11'
 '  * Bug fix in `CreateAPI`'
 '* 0.6.0 from 2023-02-11'
 '  * `CreateAPI` can now handle operators as well'
 '* 0.5.1 from 2023-02-09'
 '  * Bug fixes'
 '    * `HandleVersionNumber` did not take advantage of the right argument'
 '    * `CreateAPI` got shy results wrong'
 '* 0.5.0 from 2023-01-28'
 '  * `MakeHelpers` can now be installed as a package'
 '  * `CreateAPI` can now handle one level of sub-namespaces'
 '  * New method `FetchLaterUserCommand` added'
 '  * New method `GetPackageCfg` added'
 '  * `CopyTo` now preserves file attributes which is particularly important for time stamp comparisons'
 '  * These methods got deleted: `CreateTatinPackageFromSingleScript` and `CreateTatinPackageFromNamespace`'
 '  * Bug fixes              '
 '    * `RmDirByForce`-calls in tests flooded ⎕SE with zeros'
 '* 0.4.0 from 2022-11-15'
 '  * `RecreateFolder` now uses FilesAndDirs.RmDirByForce, making it way more successful.'
 '  * `YesOrNo` and `Select` are now called from `CommTools`'
 '  * Test cases completely automated now'
 '* 0.3.0 from 2022-10-13'
 '  * Potentially breaking change: `Select` has changed its syntax'
 '  * `MakeStandAloneRunTimeExe` did not check return code of "Bind" when it should'
 '  * Depends on the CommTools package now'
 '* 0.2.4 from 2022-09-25'
 '  * Bug fix in `MakeStandAloneRunTimeExe`: ⎕NQ must be executed in # to work'
 '  * Two test cases added and another one corrected'
 '* 0.2.3 from 2022-09-21'
 '  * `CreateZipFile` was buggy.'
 '* 0.2.2 from 2022-09-10'
 '  * `RemoveStuffButSetupExe` added'
 '  * `CreateAPI` now requires a reference rather than a name pointing to the source'
 '  * Bug fixes'
 '    * `CreateTatinPackageFromNamespace` improved including documentation'
 '    * `ConvertMarkdownToHtml5`: the left argument is now optional. The default will do most of the time'
 '* 0.2.1 from 2022-08-23'
 '  * Bug fix in `CreateAPI`'
 '* 0.2.0 from 2022-08-05'
 '  * Function `CreateTatinPackageFromNamespace` added'
 '  * Bug fixes '
 '    * `CreateTatinPackageFromSingleScript`: did not copy any asset folder over'
 '    * `CopyTo` had a problem with copying a single file into a folder with a new name'
 '    * `CreateZipFile` did not check the version number for being simple text with two dots'
 '* 0.1.1 from 2022-07-29'
 '  * `CopyBetter` should check both files for existing but did not'
 '  * `CreateAPI` did not create shy functions when required'
 '* 0.1.0 from 2022-07-24'
 '  First version, which stems from the "Maker" project which is now deprecated.'
 '  `MakeHelpers` is a Tatin package, and expected to be installed in MyUCMDs/ or a'
 '  similar folder, although all helpers are available via the API, not user commands.'
)
