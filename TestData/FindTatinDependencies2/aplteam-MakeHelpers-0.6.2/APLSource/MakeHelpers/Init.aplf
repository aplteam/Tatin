 Init
 :If '⎕SE._tatin.'{⍺≡(≢⍺)↑⍵}⍕⎕THIS    ⍝ Then it's not a project, it's loaded as a package...
     ⎕EX'MakeHelpers_UC'              ⍝ ... and in that case we do not need the user command script in the WS
 :EndIf
 A←APLTreeUtils2
 F←FilesAndDirs
⍝Done
