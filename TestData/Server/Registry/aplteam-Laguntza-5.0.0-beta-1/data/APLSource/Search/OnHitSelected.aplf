 OnHitSelected←{
     f←(,¯1)≡1↑2↓⍵      ⍝ f ←→ 1: don't reset the status bar
     ∆Ref←##.Get∆Ref 0⊃⍵
     _←DisplayCurrentSearchLine ∆Ref
     f:⍬
     ⍬⊣##.ResetStatusbar ∆Ref
⍝ Calc the selected item in the list (if any) and display it
 }
