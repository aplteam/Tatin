 SetInfoBCol←{
     ∆Ref←⍺
     0=∆Ref.⎕NC'n':⍬                ⍝ There might not be a GUI (Method, test cases...)
     color←⍵
     ∆Ref.n.infoSB.BCol←color
     ⍬
 ⍝ Cover fns for setting the Background color of the info field in the status bar
 ⍝ Specify ¯1 as ⍵ for default color.
 ⍝ Specify ∆Ref.p.statusbarAlarmColor for, well, an alarming color
 }
