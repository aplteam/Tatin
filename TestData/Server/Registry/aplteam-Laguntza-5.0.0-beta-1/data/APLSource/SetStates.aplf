 SetStates←{
     ∆Ref←⍺
     states←{2⊥⍉⌽⍵}⍵                                                 ⍝ Convert to Int
     states{2 ⎕NQ ∆Ref.n.tree'SetItemState'⍵ ⍺}¨∆Ref.d.tree[;I.Row]  ⍝ Reset all states
 ⍝ ⍵ is a matrix of Booleans with TreeView item states
 ⍝ All items are set according to ⍵ then
 }
