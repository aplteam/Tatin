 OnTitleChangeInWebBrowser←{
⍝ Forces the focus to the left pane in order to overcome the strange probem that
⍝ otherwise the browser grabs the focus back.
     ∆Ref←##.Get∆Ref 0⊃⍵
     _←⎕NQ(0⊃⍵)9991
     0
 }
