 ProgressbarCreate←{
     ∆Ref←⍵
     ref←∆Ref.n.SB.⎕NEW'Progressbar'(('Coord' 'Prop')('Posn'(0 0))('Size'(100 100)))
     ref.Limits←0 100
     ref.Thumb←1
     ref
⍝ Creates a progress bar as child of the STatusbar, hiding any other children.
 }
