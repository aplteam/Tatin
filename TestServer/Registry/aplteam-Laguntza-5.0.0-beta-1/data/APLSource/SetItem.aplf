 SetItem←{
     ∆Ref←⍺
     rf←∆Ref.n.tree                 ⍝ Ref to TreeView
     rf.SelItems←(≢rf.Items)⍴0      ⍝ Deselect all
     rf.SelItems[⍵]←1               ⍝ Select the desired one
     0
⍝ Selects ⍵ and only ⍵ in the TreeView
 }
