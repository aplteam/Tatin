 EditFromMainMenu←{
     ∆Ref←⍵
     ∆Ref.p.userMode:⍬
     ⍬≡in←GetCurrentItem ∆Ref.n:⍬      ⍝ We might not have a current item
     ⍬⊣∆Ref EditFromTv in
⍝ This function is called only from the main menu.
⍝ Because of that we cannot be sure that there is a current item.
 }
