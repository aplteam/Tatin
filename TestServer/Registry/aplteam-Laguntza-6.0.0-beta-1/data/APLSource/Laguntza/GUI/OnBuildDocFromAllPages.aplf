 OnBuildDocFromAllPages←{
     ∆Ref←⍵
     (0=≢tf←##.BuildDocFromAllPages ∆Ref):tf
     ⎕←(⎕PW-1)⍴'-'
     ⎕←'HTML file created; open this with a word processor for proof reading:'
     ⎕←tf
     tf
 }
