 GetCurrentItem←{
     n←⍵
     {⍵<≢n.tree.SelItems:⍵ ⋄ ⎕IO}⍬⍴n.tree.SelItems⍳1
⍝ Returns the number of the current item as a scalar
⍝ or ⎕IO if there is no item selected currently.
⍝ Yes, that DID happen in the past.
 }
