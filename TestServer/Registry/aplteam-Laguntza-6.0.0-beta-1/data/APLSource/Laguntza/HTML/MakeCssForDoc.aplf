 MakeCssForDoc←{
     size←⍵
     r←''
     r,←⊂'html {'
     r,←⊂'font-family: "Arial";'
     r,←⊂'font-size: 14px'
     r,←⊂'}'
     r,←⊂'hr {'
     r,←⊂'color: black;'
     r,←⊂'size 3pt;'
     r,←⊂'}'
     r,←⊂'h1,h2,h3,h4 {'
     r,←⊂'font-family: "APL385 Unicode";'
     r,←⊂'}'
     r,←⊂'h1 {'
     r,←⊂'font-size: 130%;'
     r,←⊂'font-weight: bold;'
     r,←⊂'}'
     r,←⊂'h2 {'
     r,←⊂'font-size: 110%;'
     r,←⊂'border-bottom: 1pt solid silver;'
     r,←⊂'}'
     r,←⊂'pre {'
     r,←⊂'font-family: "APL385 Unicode";'
     r,←⊂'border: 1pt solid silver;'
     r,←⊂'padding: 4pt;'
     r,←⊂'margin: 0 5pt 0 5pt;'
     r,←⊂'mso-no-proof:yes;'
     r,←⊂'}'
     r,←⊂'pre code {'
     r,←⊂'font-family: "APL385 Unicode";'
     r,←⊂'mso-no-proof:yes;'
     r,←⊂'}'
     r,←⊂'code {'
     r,←⊂'font-family: "APL385 Unicode";'
     r,←⊂'mso-no-proof:yes;'
     r,←⊂'}'
     r,←⊂'table {'
     r,←⊂'border: solid 1px silver;'
     r,←⊂'border-collapse: collapse;'
     r,←⊂'}'
     r,←⊂'td {'
     r,←⊂'border: solid 1px silver;'
     r,←⊂'}'
     r,←⊂'image {'
     r,←⊂'margin:10px;'
     r,←⊂'border: 0px;'
     r,←⊂'}'
     r
 ⍝ Creates all the CSS which is embedded into the HTML page.
 ⍝ Makes it difficult to change but makes the document self-contained.
 ⍝ This version is useful when the final doc is loaded with Word
 }
