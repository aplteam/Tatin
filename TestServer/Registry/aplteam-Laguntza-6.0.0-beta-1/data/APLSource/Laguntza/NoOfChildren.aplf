 NoOfChildren←{
     ⍺←0
     in←⍺                           ⍝ Item number
     int←⍵                          ⍝ Depth
     0=≢int:0
     ≢in WhereAreTheChildren int
⍝ Returns the number of children (but no grand-chirdren etc) found.
⍝ ⍵ is an integer vector ("Depth" of a TreeView typically)
⍝ ⍺ is the item number we are interested in. Defaults to 0.
 }
