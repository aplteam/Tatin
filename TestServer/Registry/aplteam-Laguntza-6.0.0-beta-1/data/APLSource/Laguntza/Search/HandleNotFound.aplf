 HandleNotFound←{
     ∆Ref←⍵
     ##.IsNotOnSearchTab ∆Ref:⍬
     _←##.ShowEmptyPage ∆Ref
     _←ShowNotFound ∆Ref
     ⍬
 ⍝ Make sure that any emphasized stuff is removed from the search screen
 ⍝ and then display a message to the user that nothing was found.
 }
