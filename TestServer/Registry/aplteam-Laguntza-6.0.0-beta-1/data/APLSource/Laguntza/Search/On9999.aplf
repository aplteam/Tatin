 On9999←{
     ∆Ref←##.Get∆Ref 0⊃⍵
     n←∆Ref.n
     ef←0<≢n.searchString.Text  ⍝ ef = empty flag. True when nothing to search for
     n.searchBtn.Active←ef      ⍝ Make "Go" (search) btn active if appropriate
     ##.ResetStatusbar ∆Ref     ⍝
⍝ Checks the length of the string entered in the "Combo".
⍝ If it's empty, make the "Search" button inactive
⍝ If not, make button active
⍝ Reset status bar in any case
 }
