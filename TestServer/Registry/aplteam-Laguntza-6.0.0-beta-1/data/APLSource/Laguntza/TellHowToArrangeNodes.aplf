 TellHowToArrangeNodes←{
     ∆Ref←⍵
     ⍺←0
     testflag←⍺
     testflag:r←1
     msg←''
     msg,←⊂'Note that by default new nodes/help pages are inserted'
     msg,←⊂'in alphabetical order.'
     msg,←⊂''
     msg,←⊂'You can change the order to your liking by selecting:'
     msg,←⊂'"Manage ∆TopicProperties"'
     msg,←⊂'from the context menu of the tree view.'
     1:r←1⊣∆Ref.p.helpCaption Show msg
 }
